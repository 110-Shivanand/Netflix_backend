# ── Production dependencies ───────────────────────────────────
# Pinned to exact versions for reproducible Docker builds.

fastapi==0.115.6
uvicorn[standard]==0.32.1
httpx==0.28.1
python-dotenv==1.0.1
prometheus-fastapi-instrumentator==7.1.0
